---
status: open
opened: 2026-08-10
---
# Findings-first report skeleton, from a `report-structure` pass at a publish gate

The pass found the residual duplication in an otherwise clean report is generated by two mechanisms: a standalone `## Hypotheses` block (every gate is stated there *and* in Findings *and* in the section verdict *and* often a caption) and rationale living in Method rather than at first use. Proposal, held rather than applied because it would restructure an already-frozen prereg skeleton post hoc:

**Target running order:** tl;dr → Findings → how-to-read admonition (the frozen-commit hash becomes the prereg attestation) → background compressed to ~250 words → glossary + the conditions table (the one Method element results can't be read without) → results, one section per hypothesis, each *opening* with its frozen prediction (gate, partial band, contrary outcomes), then evidence, then verdict → exploratory → discussion, implications only (~200 words) → Method/Calibration/Measurements deferred to the end.

**Three authoring rules that prevent the duplication at the source:** a prediction lives at the top of its own result section, never in a separate block; a rationale lives at the point of first use, with pointers elsewhere; the Discussion may only reference results, never requote them.

**What breaks, and mitigations:** forward references from results into Measurements definitions — the glossary must define every term the verdicts use (a well-kept glossary already does); Calibration cells read oddly in past position — reword their frame ("before anything ran…"), not their content; the reader loses the four predictions read consecutively — the Findings block carries that in verdict form, and the how-to-read note's commit hash carries the "this was frozen" role the monolithic block served visually. Estimated effect on a ~6,000-word report: ~6,000 → ~4,500 words, first evidence ~700 words after the title instead of ~2,800.


## Notes

**2026-09-04, backport** — Ported from sca2 ([`todo/style/findings-first-report-skeleton-from-report-structure.md`](https://github.com/z0u/sca2/blob/main/todo/style/findings-first-report-skeleton-from-report-structure.md) there), where it was written at ex-2.1.10's publish gate and is shortlisted for that project's next preregistration. Here it is aimed at the `science` skill's skeleton guidance and the `prereg-reviewer`'s expectations, both of which this template ships. One of the three rules ("keep rationales, caveats, and worked reasoning at the point of use rather than in the method") is already in the skill; the prediction-at-the-top-of-its-own-section rule, the Discussion-may-reference-but-not-requote rule, and the deferral of Method/Calibration/Measurements to the end are what's left to write in.
